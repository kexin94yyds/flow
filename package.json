{
  "name": "card-bookmarks",
  "version": "1.0.0",
  "description": "卡片式书签 Chrome 扩展",
  "scripts": {
    "build": "tsc && cp src/popup/popup.html src/popup/popup.css dist/popup/ && cp src/options/options.html src/options/options.css dist/options/ && cp manifest.json dist/ && cp -r icons dist/",
    "watch": "tsc --watch"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
